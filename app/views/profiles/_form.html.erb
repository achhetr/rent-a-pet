<%= simple_form_for @user, url: profile_path(@user.id) do |f| %>
  <%= f.input :name %>
  <%= f.input :mobile %>
  <%= f.input :location %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>